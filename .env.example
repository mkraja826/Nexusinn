# Public production URL used for canonical URLs, sitemap and same-origin contact validation.
NEXT_PUBLIC_SITE_URL=https://example.com

# Server-only contact delivery destination. Do not prefix with NEXT_PUBLIC_.
CONTACT_WEBHOOK_URL=

# Optional bearer secret sent from Nexusinn to the configured webhook.
CONTACT_WEBHOOK_SECRET=
